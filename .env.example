# ============================================
# SISTEMA LOCAL CON SQLITE - TEMPLATE
# ============================================

# Secretos para JWT y tokens QR (CAMBIAR EN PRODUCCIÓN)
JWT_SECRET=genera_un_secreto_aleatorio_aqui
HMAC_SECRET=genera_otro_secreto_aleatorio_aqui

# Puerto del servidor (opcional, por defecto 5000)
PORT=5000

# Entorno (development, production, test)
NODE_ENV=development

# ============================================
# NOTAS IMPORTANTES
# ============================================
# - DATABASE_URL no es necesario, el sistema usa SQLite automáticamente
# - Ubicación de la base de datos: prisma/dev.db
# - Para generar secretos seguros, usa: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
